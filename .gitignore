# Mac
.DS_Store

# Brand assets (large files, keep local only)
Brightseed brand assets from 2022/
/brand/

# Personal memory (decoded shorthand, people notes, internal reasoning)
memory/

# Working folders, local only. Curate into DESIGN.md / Storybook — not a new doc.
OUTPUTS/
vector-pipeline/
DOCS/archive/
anna's mocks 4-29-26/

# Reference PDF, local only. DOCS/ is retired — if any of this is worth keeping,
# it belongs in DESIGN.md or a Storybook page, not a new doc.
Hummingbird reference ui.pdf

# Claude Code local state (worktrees are multi-GB)
.claude/

# Marketing / brand asset exports (large; keep local, like /brand)
hummingbird.website/

# Generated artifact, local only. Regenerate rather than commit; DOCS/ is retired.
design-system-architecture-diagrams.html

# Stray root package — the app's package.json lives in web/
/package.json
/package-lock.json

# --- standard ---
node_modules/
.next/
.vercel/
.env*
*.log
.DS_Store

# Agent scratch context (superseded by CLAUDE.md + memory/)
.agents/

# Portfolio + raw meeting notes: local working material, not client-facing
DOCS/case-study-draft.md
DOCS/notes after chat with anna.txt
collaboration/onboarding-anna.md
