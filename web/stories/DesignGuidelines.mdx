import { Meta, Markdown } from "@storybook/addon-docs/blocks";
import source from "./design-source.md?raw";

{/*
  This page is a RENDERER, not a source.

  The canonical design guidance is DESIGN.md at the repo root — the Google
  design.md format file that coding agents read
  (https://github.com/google-labs-code/design.md). `design-source.md` is a
  symlink to it.

  This file used to be a second, hand-maintained copy of the same guidance and
  the two drifted: by July 2026 each held content the other lacked (the root
  file had the entire Layout & Spacing section, this one had the corrected
  2xs/xs radius steps), and they disagreed about which surface the sidebar
  uses. Rendering the single source removes that failure mode — this page can
  no longer contradict what agents read.

  Edit DESIGN.md at the repo root. Nothing here needs to change.

  The YAML front matter (machine-readable tokens) is stripped before render:
  it is for agents, not readers. Everything after it is shown verbatim.
*/}

<Meta title="Design.mdx" />

<Markdown>{source.replace(/^---\r?\n[\s\S]*?\r?\n---\r?\n/, "")}</Markdown>
